#include <iostream>

using namespace std;

int main() {
    int a = 10;
    int b = 5;

    if (b > a) {
        cout << "b is larger than a";
    } else {
        cout << "a is larger than b";
    }

    int time = 20;
    if (time > 12) {
        cout << "\nGood afternoon";
    } else {
        cout << "\nGood morning";
    }
    return 0;
}