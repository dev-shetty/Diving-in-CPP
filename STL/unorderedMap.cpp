#include <bits/stdc++.h>
using namespace std;
int main() {
    // Same as map and has unique keys but not in sorted order
    unordered_map<int, int> um;

    // Functions - O(1) and worst case (rare case) - O(N)
    return 0;
}